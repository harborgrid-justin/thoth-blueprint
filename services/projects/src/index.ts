/**
 * @thoth/service-projects — project lifecycle & persistence.
 *
 * SCAFFOLD. See ./README.md. Owns projects, server-side persistence, versioning,
 * checkpoints (named snapshots with restore), and the audit trail.
 */

export const __SCAFFOLD__ = true;
