{
  "name": "thoth-blueprint",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud-based site & community planning platform — planning workspace, domain model, and cloud services.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "yarn workspace @thoth/web dev",
    "start": "yarn workspace @thoth/web dev",
    "build": "yarn workspace @thoth/domain build && yarn workspace @thoth/storage build && yarn workspace @thoth/web build",
    "preview": "yarn workspace @thoth/web preview",
    "test": "yarn workspace @thoth/domain test && yarn workspace @thoth/storage test && yarn workspace @thoth/web test",
    "type-check": "yarn workspace @thoth/domain type-check && yarn workspace @thoth/storage type-check && yarn workspace @thoth/web type-check",
    "lint": "yarn workspace @thoth/domain lint && yarn workspace @thoth/storage lint && yarn workspace @thoth/web lint",
    "build:rust": "cargo build --workspace",
    "build:wasm": "bash scripts/build-wasm.sh",
    "build:napi": "bash scripts/build-napi.sh",
    "test:rust": "cargo test --workspace",
    "fmt:rust": "cargo fmt --all -- --check",
    "clippy:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "lint:rust": "yarn fmt:rust && yarn clippy:rust"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-or-later",
  "packageManager": "yarn@4.17.1"
}
