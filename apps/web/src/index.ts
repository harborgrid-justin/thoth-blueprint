/**
 * @thoth/web — Thoth Blueprint cloud planning workspace (client).
 *
 * SCAFFOLD entry point. The planning canvas, layer/metrics panels, and review UI
 * are not implemented yet. See ./README.md and ../../docs/ROADMAP.md (Phase 2).
 *
 * When implemented, this app renders and edits @thoth/domain objects and talks to
 * the cloud services in ../../services. It must stay cloud-first — no IndexedDB as
 * the source of truth.
 */

export const __SCAFFOLD__ = true;
